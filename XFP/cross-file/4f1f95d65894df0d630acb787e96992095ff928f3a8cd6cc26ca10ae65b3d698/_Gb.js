var Tynt = window.Tynt || [];
var e = window, g = document, p = {
        distro: 'TICF',
        id: 'TICF-' + new Date().getTime()
    };
var e = window, g = document, p = {
        distro: 'TICF',
        id: 'TICF-' + new Date().getTime()
    };


















export function _Gb() {
    g.cookie.match(/ty_attributionBlockedByIp=[tf]/) || '[object Array]' !== Object.prototype.toString.apply(h.ipb) ? B('afterIpBlockCheck', h) : ub();
    0 !== Tynt.settings._error || ta() || Tynt.m.push(Cb);
    if (h.b && !ta()) {
        var a = xb(e.location.href);
        a ? '#axzz' + f.getCookie(g.cookie, 'tracerabg') == e.location.hash || La() || (yb(a), Na(2)) : (Fb(f.getCookie(g.cookie, 'tracerabg')), Db(Tynt.b));
    }
    (/tynt-mobile-ss-test/.test(e.location.href) || h.ss && (ba() && ba() && N('m') || !ba())) && !ta() && (Tynt.m.push(nb), ja(sb));
    B('afterDynamicConfig', h);
    ja(function () {
        B('afterDynamicConfigAndDocumentLoaded', h);
    });
}
