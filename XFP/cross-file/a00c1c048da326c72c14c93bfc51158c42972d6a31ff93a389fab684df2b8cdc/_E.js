export function E(t, o) {
	var e = localStorage.getItem("subscription");
	return (
	  !!localStorage &&
	  ("vapid" == o &&
		((t = JSON.parse(JSON.stringify(t))), (t = JSON.stringify(t))),
	  t == e)
	);
  }